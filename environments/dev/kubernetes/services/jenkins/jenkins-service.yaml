apiVersion: v1
kind: Service
metadata:
  name: jenkins
  namespace: jenkins
#  annotations:
  labels:
    Application: jenkins
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 8080
  selector:
    Application: jenkins
  type: LoadBalancer
