apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-ebs
  namespace: jenkins
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  selector:
    matchLabels:
      Name: jenkins-ebs
  storageClassName: gp2
  volumeName: jenkins-ebs